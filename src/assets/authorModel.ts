export class authorModel {
    constructor(
      public firstName: string,
      public lastName: string,
      public authorSummary: string,
      public region: string,
      public email: string
    ) {  } 
}